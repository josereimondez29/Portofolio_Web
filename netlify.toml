[build]
  base = "frontend/"
  command = "npm install && npm install -g tailwindcss && npm run build"
  publish = "dist"

[[redirects]]
  from = "/api/*"
  to = "https://josereimondez-portfolio-backend.onrender.com/api/:splat"
  status = 200
